# tetrix-hacaton
Task 1. Smart-Wine shop network

## Building

### Once:

```mkdir build && cd build && cmake ..```

### Each build:

In project root:

```make -C build```

Run: 

```./build/start```

